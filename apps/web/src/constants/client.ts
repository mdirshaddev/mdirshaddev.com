/**
 * It's checking if the window object is defined.
 */
export const isClient = typeof window !== 'undefined';

/**
 * It's checking if the system color is dark.
 */
export const MEDIA = '(prefers-color-scheme: dark)';
